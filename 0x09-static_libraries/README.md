#Static libraries

* file libholberton.a is a static library containing previous functions
* create static lib : is a script that creates a static library fromm all the c files in the current directory
